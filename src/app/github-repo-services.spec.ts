import { GithubRepoServices } from './github-repo-services';

describe('GithubRepoServices', () => {
  it('should create an instance', () => {
    expect(new GithubRepoServices()).toBeTruthy();
  });
});
